---
import {
  Bot,
  Library,
  MessageSquareText,
  Quote,
  SearchCheck,
  ShieldCheck,
  Sparkles,
  Volume2,
  WifiOff,
} from "@lucide/astro"
import { RagmirBackground } from "@jcode.labs/ragmir-ui"
import { CommandCopyBox, CommandCopyPill } from "../command-copy"

interface Props {
  translations: Record<string, string>
}

const { translations } = Astro.props
const t = (key: string): string => translations[key] ?? key

const featureGroups = [
  {
    icon: Library,
    title: t("features_core_title"),
    installCommand: "npm install -D @jcode.labs/ragmir",
    command: "npx rgr setup",
    text: t("features_core_text"),
    items: [
      { icon: SearchCheck, text: t("features_core_item_1") },
      { icon: Quote, text: t("features_core_item_2") },
      { icon: Bot, text: t("features_core_item_3") },
      { icon: ShieldCheck, text: t("features_core_item_4") },
      { icon: WifiOff, text: t("features_core_item_5") },
    ],
  },
  {
    icon: Volume2,
    title: t("features_tts_title"),
    installCommand: "npm install -D @jcode.labs/ragmir-tts",
    command: "npx rgr audio",
    text: t("features_tts_text"),
    items: [
      { icon: Volume2, text: t("features_tts_item_1") },
      { icon: Quote, text: t("features_tts_item_2") },
      { icon: WifiOff, text: t("features_tts_item_3") },
      { icon: ShieldCheck, text: t("features_tts_item_4") },
      { icon: Sparkles, text: t("features_tts_item_5") },
    ],
  },
  {
    icon: MessageSquareText,
    title: t("features_chat_title"),
    installCommand: "npm install -D @jcode.labs/ragmir-chat",
    command: "npx rgr chat setup",
    text: t("features_chat_text"),
    items: [
      { icon: MessageSquareText, text: t("features_chat_item_1") },
      { icon: Quote, text: t("features_chat_item_2") },
      { icon: WifiOff, text: t("features_chat_item_3") },
      { icon: Bot, text: t("features_chat_item_4") },
      { icon: ShieldCheck, text: t("features_chat_item_5") },
    ],
  },
]
---

<section
  id="features"
  aria-labelledby="features-heading"
  class="container-wide relative z-10 px-5 py-24 md:px-8 md:py-36"
>
  <RagmirBackground client:visible behindContent className="inset-0" height="100%" />

  <div class="mb-10 grid gap-5 lg:grid-cols-[0.78fr_1.22fr] lg:items-end">
    <div>
      <p class="text-xs font-bold uppercase tracking-[0.18em] text-muted-foreground">
        {t("features_eyebrow")}
      </p>
      <h2 id="features-heading" class="mt-4 font-black text-2xl leading-tight md:text-3xl">
        {t("features_title")}
      </h2>
    </div>
    <p class="max-w-3xl text-muted-foreground text-sm leading-6 lg:justify-self-end">
      {t("features_text")}
    </p>
  </div>

  <div class="grid gap-4 lg:grid-cols-3">
    {featureGroups.map((group) => (
      <article class="flex min-h-full flex-col rounded-xl border border-border bg-card/82 p-5 text-card-foreground shadow-xl shadow-black/20 backdrop-blur-xl md:p-6">
        <div class="flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between">
          <div class="flex size-11 items-center justify-center rounded-lg border border-border bg-muted/60">
            <group.icon class="size-5 text-[var(--accent-title)]" aria-hidden="true" />
          </div>
          <div class="min-w-0 sm:max-w-[17rem]">
            <CommandCopyPill
              client:visible
              command={group.installCommand}
              copyLabel={t("copy_command")}
            />
          </div>
        </div>
        <h3 class="mt-5 font-black text-xl leading-tight">{group.title}</h3>
        <p class="mt-3 text-muted-foreground text-sm leading-6">{group.text}</p>
        <div class="mt-5">
          <CommandCopyBox
            client:visible
            command={group.command}
            copyLabel={t("copy_command")}
          />
        </div>
        <ul class="mt-5 grid gap-3">
          {group.items.map((item) => (
            <li class="flex gap-3 text-sm leading-6">
              <item.icon class="mt-0.5 size-4 shrink-0 text-muted-foreground" aria-hidden="true" />
              <span class="text-foreground/88">{item.text}</span>
            </li>
          ))}
        </ul>
      </article>
    ))}
  </div>
</section>
