{
  "name": "@jcode.labs/mimir-license-webhook",
  "version": "0.4.10",
  "private": true,
  "description": "Private Cloudflare Worker handler for Mimir Desktop license webhooks.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "smoke": "pnpm build && node scripts/smoke.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
